export class TournamentResultView {
  ;

    constructor(public tournamentId: string, public tournamentName: string, public placing: number, public rebuyAmount: string, public currency: string, public canRebuy:boolean) {
  
    }
  
  }